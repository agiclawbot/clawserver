apiVersion: v1
kind: Service
metadata:
  name: clawserver
  labels:
    app: clawserver
spec:
  type: ClusterIP
  ports:
    - port: 3385
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: clawserver
